[package]
name = "moonlit-libobs-protocol"
version = "0.1.0"
description = "Versioned control protocol for the MoonLit libobs recorder"
license = "GPL-3.0-only"
edition = "2021"
rust-version = "1.88"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
